#pragma once

#include "util.h"


RICH_ENUM(TechVariant,
  DARK,
  WHITE
);

RICH_ENUM(WorkerVariant,
  IMP,
  PEASANT_PRISONER,
  PEASANT
);

RICH_ENUM(TribeAlignment,
  LAWFUL,
  EVIL
);

RICH_ENUM(ImmigrantsVariant,
  LAWFUL,
  EVIL
);
